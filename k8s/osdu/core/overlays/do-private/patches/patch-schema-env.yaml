apiVersion: apps/v1
kind: Deployment
metadata:
  name: osdu-schema
spec:
  template:
    spec:
      containers:
        - name: schema
          env:
            - name: PARTITION_API
              valueFrom:
                configMapKeyRef:
                  name: osdu-core-env
                  key: PARTITION_API
