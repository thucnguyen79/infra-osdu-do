apiVersion: v1
kind: Service
metadata:
  name: entitlements
  labels:
    purpose: alias-for-secret-service
spec:
  selector:
    app: osdu-entitlements
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
