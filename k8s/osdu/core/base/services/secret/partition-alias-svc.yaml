apiVersion: v1
kind: Service
metadata:
  name: partition
  labels:
    purpose: alias-for-secret-service
spec:
  selector:
    app: osdu-partition
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
