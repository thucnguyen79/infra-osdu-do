apiVersion: v1
kind: Service
metadata:
  name: register
  labels:
    app: osdu-register
    app.kubernetes.io/name: register
    app.kubernetes.io/part-of: osdu
spec:
  selector:
    app: osdu-register
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
